# EVM path

`blockchain/contracts/FingerprintRegistry.sol` is a minimal reference contract for a public EVM deployment. Before deploying, add a reviewed provider, RPC configuration, wallet key management, network-specific tests, and a transaction explorer link. Never place biometric data on-chain.

