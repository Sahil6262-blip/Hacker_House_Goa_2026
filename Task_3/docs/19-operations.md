# Operations

Monitor `/api/health`, provider failures, upload rejection counts, and ledger integrity failures. Back up the local ledger only for development use. Never log API keys, raw images, descriptors, or full sensitive source data. Rotate the Bing key if exposed.

