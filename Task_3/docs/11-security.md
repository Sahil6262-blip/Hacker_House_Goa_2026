# Security

Validate upload media types and size, keep credentials in `.env`, serve production API/frontend over HTTPS, restrict CORS to deployed origins, rate limit externally exposed endpoints, and use a managed secret store. The sample has no authentication; do not expose it as a multi-user service without adding auth and abuse controls.

