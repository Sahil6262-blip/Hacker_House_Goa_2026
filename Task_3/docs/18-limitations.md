# Limitations

Haar detection has demographic and image-quality limitations. The prototype descriptor is not a production face-recognition embedding. Search availability, result ordering, and candidate image links depend on an external provider. The default ledger is local rather than EVM. A production build needs independent evaluation, human review, retention controls, authentication, and a real public-chain provider where appropriate.

