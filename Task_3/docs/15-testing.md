# Testing

Backend tests cover stable SHA-256 fingerprint creation, fingerprint change on source change, ledger write/read/verify, and failed verification after tampering. Run `scripts/check.ps1` to execute backend tests, Ruff, mypy, and the frontend production build.

