# Candidate comparison

When a provider supplies a public candidate image URL, the backend fetches it, detects a face, and compares descriptors. Missing/inaccessible candidate images are reported as such. Similarity does not establish identity or source ownership.

