# Local ledger

Each record includes the proof hash, source URL, timestamp, block number, previous block hash, transaction hash, and block hash. Verification recomputes both hashes. The demo tamper endpoint changes a record only to demonstrate verification failure. The ledger is local development storage, not a public chain.

