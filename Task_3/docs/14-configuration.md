# Configuration

Copy `.env.example` to `.env`. `BING_VISUAL_SEARCH_KEY` enables genuine search. `LOCAL_LEDGER_PATH` chooses a development-ledger location. `ALLOWED_ORIGINS` accepts a comma-separated frontend origin list. Frontend uses only `VITE_API_URL`.

