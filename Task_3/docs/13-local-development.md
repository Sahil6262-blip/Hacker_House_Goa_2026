# Local development

Run `scripts/setup.ps1`, then start backend and frontend in separate PowerShell windows. The frontend runs at port 5174 and backend at port 8001, avoiding Task 2’s ports. See the root Task 3 README for exact commands.

